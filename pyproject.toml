[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "gjp-erp-billing-mcp"
version = "0.1.0"
description = "基于 MCP Python SDK 的 ERP AI 业务 MCP 服务"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cachetools>=5,<6",
    "cryptography>=44,<51",
    "httpx>=0.27,<1",
    "jsonschema>=4.25,<5",
    "mcp>=2.2,<3",
    "pydantic>=2.10,<3",
    "PyJWT>=2.8,<3",
    "starlette>=0.41,<1",
    "uvicorn>=0.30,<1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8,<10",
    "ruff>=0.12,<1",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/erp_billing",
    "src/gjp_common",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
